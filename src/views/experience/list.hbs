<div class="container p-4">
    <div class="row">
        
        {{#each experience}}
            <div class="col-md-3">
                <div class="card bg-light mb-3">
                    <div class="card-body">
                            <h3 class="card-title text-uppercase">
                                {{title}}
                            </h3>
                        </a>
                        <p class="m-2">{{description}}</p>
                        <a href="/experience/edit/{{id}}" class="btn btn-outline-secondary">Edit</a>
                        <a href="/experience/delete/{{id}}" class="btn btn-outline-danger">Delete</a> 
                    </div>
                </div>
            </div>   
        {{/each}}
    </div>
    <div class="row col-md-4 mx-auto">
        <a href="/experience/add" class="btn btn-info btn-block text-uppercase">Add Experience<a>
    </div>
</div>